<Window x:Class="HolaAI.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hola-AI Settings"
        Width="520"
        Height="330"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Loaded="Window_OnLoaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="OpenRouter API Key"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4" />
        <TextBox x:Name="ApiKeyBox"
                 Grid.Row="1"
                 Height="30"
                 VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="2"
                   Text="STT Model"
                   FontWeight="SemiBold"
                   Margin="0,12,0,4" />
        <TextBox x:Name="SttModelBox"
                 Grid.Row="3"
                 Height="30"
                 VerticalContentAlignment="Center" />

        <StackPanel Grid.Row="4"
                    Margin="0,12,0,0">
            <TextBlock Text="Prompt Model"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4" />
            <TextBox x:Name="PromptModelBox"
                     Height="30"
                     VerticalContentAlignment="Center" />

            <CheckBox x:Name="ShowOverlayCheck"
                      Margin="0,12,0,0"
                      Content="Show overlay on startup"
                      IsChecked="True" />
        </StackPanel>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Width="90"
                    Height="32"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Content="Cancel" />
            <Button Width="90"
                    Height="32"
                    IsDefault="True"
                    Click="SaveButton_OnClick"
                    Content="Save" />
        </StackPanel>
    </Grid>
</Window>
